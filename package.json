{
  "name": "ai-web-search-agent",
  "version": "1.0.0",
  "description": "AI web search agent challenge implementation in Node.js",
  "type": "module",
  "main": "agent/index.js",
  "scripts": {
    "start": "node agent/index.js",
    "start:web": "node server.js",
    "check": "node --check agent/index.js && node --check agent/runAgent.js && node --check agent/search.js && node --check agent/fetchPageContent.js && node --check agent/buildAnswer.js && node --check agent/config.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "dotenv": "^16.4.7",
    "duck-duck-scrape": "^2.2.7"
  }
}
